# Changelog

本文件按版本记录「在线密码本」的变更（§14.1 一期收口要求）。

## v0.1.0（一期 MVP 交付）

一期 = 服务端 + 客户端核心库（加解密/同步/冲突合并）+ 标准 UI 壳 + 管理端，
配功能驱动最小 UI（密码生成器、自动锁定、DPAPI 自动解锁）。

### 功能

- **加密体系**：国密 SM2/SM3/SM4 全链路（keyfile 口令派生 KEK、SM2 包裹 DEK 信封、
  SM4-GCM 条目加密 + HMAC-SM3 防伪章），wire format 带版本前向兼容。
- **服务端**：TLS 1.3；bootstrap 一次性引导、设备注册（challenge + SM2 签名核销）；
  同步（push/pull 增量 + SSE 推送 + 回退轮询）；admin API（用户/组/成员/设备/审计/
  rekey/归档）；限流、审计、墓碑清理。
- **客户端核心库 `client/core`**：vault（解锁/DEK 缓存/加解密）、syncer（SSE + 轮询 +
  auto-wrap 冷启动 + auto-rekey + 写挂起）、merge（字段级三路合并）、store（本地 SQLite）。
- **普通客户端 `client/app`**（Wails）：解锁/列表/编辑/冲突逐字段解决/设置页面；
  密码生成器；失焦 5 分钟自动锁定（含倒计时提示）；Windows DPAPI 自动解锁。
- **管理面板（`client/app` 内，`--admin` 模式）**：引导/组管理/成员清单（在线状态/设备）/成员开户与移除/设备（主机信息）/审计/密钥生成。
- **CLI**：`keytool`（genuser/pubkey/inspect）；`pbcli`（unlock/bootstrap/admin/
  put/delete/sync/list/genpass，含设备注册与 CA pinning）。

### 安全

- 服务端零明文（不定义/不记录任何明文敏感字段类型与日志）。
- 密钥一律 `[]byte` 驻留，锁定/退出覆写清零（Wipe）。
- 随机数仅 `crypto/rand`；密钥不跨层到前端 JS。
- 自签 CA + 客户端内置 CA 信任（pinning）支持（§8.3）。
- gosec 0 告警；go vet 零告警；`go test -race` 无数据竞态。

### 验收

- 一期 16 场景端到端验收：bootstrap / 建用户加组 / 双端同步 / rekey / 吊销 /
  归档重启 / 成员清单 / 篡改密文拒收等核心场景在 Linux 裸机实机通过（`docs/验收/一期-e2e.md`）。
- DPAPI 自动解锁（场景 14）Windows 实机单测闭环通过；失焦锁定/锁屏清零（场景 9/10）
  为 Windows 客户端 UI 行为，实机走查待补。
- 性能基线：解锁 ≤0.3s、单轮同步 ≤0.3s、服务端空闲内存 ≤20MB（详见验收文档）。

### 修复

- P1：auto-rekey 中断恢复时重加密只匹配 `key_version == oldKV`，本地条目停留在更旧
  kv 时每轮 kv+1、pending_rekey 永不收敛的死循环（改为重加密所有未达 newKV 的条目）。
- P0：token 二次解密导致 Unlock 必败。
- P1：auto-rekey 包裹对象错误（多组场景信封含非组成员，rekey 永不收敛）。
- 其余 P2/P3：密钥卫生、并发安全、错误码映射、半解锁态回滚等累计 15+ 项。

### 修复（2026-09-11 本机 e2e 实机实测发现）

- **P1｜注册失败回滚失效**：`UnlockView` 回滚调用的 `api.ClearIdentity` 前端门面未导出
  （生成的绑定存在，手写 `api.js` 漏了）→ 回滚抛 TypeError、本地身份残留，用户被
  「本地已有身份，不能重复注册」拦死且无 token/无 keyfile 可用。同时 `core.ClearIdentity`
  只清库未复位内存身份（`c.role/c.username`），同进程内仍被判为已有身份。已补门面导出 +
  内存复位 + 失败态提示与 UI 复位（可原地重试）。
- **P1｜管理面板轮询吃光 Admin 配额**：`pollTab` 的 groups/open 分支走 `refresh()`，
  一次扇出 4 个 admin 请求（overview 5 个）= 48~60 次/分，超 §8.3 的 30/min/token，
  导致审核/开户/生成邀请码等写操作间歇性 42901。改为按 tab 单请求（概览 20s 全量）+
  60s 滑动窗口轮询预算 18 次（为写操作留 ≥12）+ 窗口不可见暂停；单客户端最坏 12 次/分。
- **P1｜注册申请端点误并入认证桶（20 客户端即触发）**：`/auth/register-request`、
  `/auth/register-status` 被放在「认证 5/min/IP」下，而 §8.3 只覆盖
  bootstrap/device-challenge/device。待审客户端每 30s 轮询状态（2 次/分/客户端），
  20 人同处一个 NAT 出口时仅 5 个待审就耗尽配额，连带把全团队的 device-challenge/
  注册/登录一起拒掉。按 §8.3 拆出「注册申请 60/min/IP」（`PB_RATE_REGISTER`），
  认证面 5/min/IP 与防暴破锁 IP 策略不变。
- **P2｜组成员缺 role**：`GroupMemberInfo` 未透出 `role`（`ListGroupMembers` 已 JOIN 出
  `u.role`，组装 DTO 时丢失）→ 管理员在组内成员列表仍显示「移除/吊销」。已补 `role` 字段。
- **P2｜设备页刷新不刷新**：顶栏「⟳ 刷新」只调 `refresh()`（不含 `loadDevices`），
  且进设备 tab 仅在未加载时拉一次 → 设备列表永远停在旧快照。已补 `refreshAll()`。
- **P2｜空状态/缺组提示误导**：未入组时建条目提示「缺少组 ID（…请检查服务端连接）」
  （与连接无关）；左栏空状态写死「＋ 新建环境」而工具栏按钮实为「＋ 新建项目」。
  已改为可操作引导（请管理员入组 → 点同步后重试）与随上下文切换的文案。

### 修复（2026-09-12 补删除入口时实测发现）

- **P0｜删除条目无法同步**：拉取落地（`syncer.applyOneChange`）构造本地行时**漏带 `Deleted`**
  （Go 零值 false），且墓碑未短路字段级三路合并。删除后紧接着同步时，服务端会把「本端刚推上去的
  那一版」又发回来（推送成功后 `last_seq` 只在拉取时推进），于是本地墓碑被合并/冲突流程抹成普通
  条目或冲突态：**删除永远推不出去、服务端与他人仍持有密文**，而界面给出的是绿色「已删除」提示
  （静默失败 + 误导反馈）。
  已改为**墓碑优先**：本机处于「已删除待推送」时，任何拉取到的内容不得覆盖该意图（墓碑无明文素材，
  不参与三路合并）；推送被对端改动判为冲突（40901）时同样保持删除意图，只把基准推进到服务端当前
  版本、下轮重推。补 3 条回归用例（**未修复时全部失败**，覆盖"拉取不覆盖 / 墓碑必被推送 / 冲突不复活"）。
  实机验证：删除 → 同步 → 服务端 `deleted=1` 且密文清空；其他成员同步后本地副本移除、密文进回收站。
- **新增｜客户端账号删除入口**：账号弹窗（密码本列表页与工作台共用）增加「🗑 删除此账号」
  （危险色、置于最左，避免与主操作相邻误触），复用「级联墓碑 + 二次确认（含子树条数提示）」。
  **管理端不提供该入口**——按 §14.1「管理接口不赋予内容特权」，条目删除一律走客户端墓碑语义，
  不提供服务端物理删行接口。
- **新增｜树节点操作入口**：左树的项目标题行与环境行悬停时显示「✏ 改名/编辑」与「🗑 删除」
  （沿用既有悬停快捷操作约定；改为行内操作组，避免多按钮互相遮挡；悬停时隐藏右侧计数让位）。
  改名复用同一编辑表单（编辑态归属链只读）；删除复用同一套级联墓碑与二次确认，
  确认框会提示「将连同其下 N 条子条目一并删除」。IP 行为按账号 IP 字段聚合出的视图层，
  不是数据节点，故不提供节点级改名/删除。

### 修复（2026-09-12 覆盖度补测：keyfile 与新设备恢复）

- **P1｜新设备「导入私钥恢复身份」后无法完成设备注册**：解锁页的「口令解锁 / 邀请码注册 /
  手动生成密钥」三条流程都会先把服务端地址与 CA 持久化到本地库，**唯独私钥导入流程没有**，
  于是导入成功后点「注册设备并进入」报「未配置服务端地址」——用户在界面上明明填了地址，
  却被告知未配置，新设备恢复登录流程走不完（keyfile 备份的用途被堵死）。
  已按同一写法补上持久化（并在地址为空时给出明确提示「请先填写服务端地址」）。
  实测：导出备份 → 换新设备导入 → 身份恢复 → 注册设备 → 进入主界面并拉取解密原有 8 条条目。

### 优化（2026-09-12 全量回归后的细节收口）

- **破坏性操作的确认按钮改用危险色**：管理端通用确认弹窗新增 `danger` 选项——删除组 / 移除成员 /
  吊销成员 / 拒绝注册申请 的确认按钮由主色（蓝）改为危险色（红），与既有危险操作视觉一致；
  「通过并开户」等正向操作保持主色。
- **冲突解决页可查看明文**：三路比对（基础 / 本地 / 服务端）原先密码字段一律显示 `••••••••`，
  用户无法判断该保留哪一份。新增「👁 显示明文 / 🙈 隐藏明文」切换（与账号详情页 👁 用法一致），
  逐字段选择按钮内的取值同步解除掩码（仅在已解锁界面生效）。
- **解锁页「验证」提示语对齐实际行为**：原提示「服务端连接正常，配置已就绪」易被理解为地址已保存，
  而该操作只做连通性检查（地址由解锁 / 注册 / 导入 / 生成密钥各流程分别保存）。改为
  「服务端连接正常（地址会在继续操作时自动保存）」。

## 未收录（后续版本规划）

- 二期 UI 打磨、三期可选功能（TOTP/附件/备份回收站/Windows Hello/更新推送等）。
